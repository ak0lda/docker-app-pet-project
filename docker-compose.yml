
services:
  web: 
    image: nginx:alpine
    container_name: nginx_static_blog
    ports:
      - "1313:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped 

    